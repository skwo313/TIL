# 주요학습범위
1. 함수 사용자정의함수, 내장함수
2. 객체 내장객체
3. DOM 객체

# 생각해봅시다.
1. js에서는 객체의 생성방법이 크게 두가지로 나누어 지는데, 이것의 활용은 어떻게 나누어지는가?
	- function은 통한 생성자형 객체 선언/class를 통한 객체 선언
		var 참조명 = new 선언한생성자명();
		고급객체 활용에서 사용된다.
		node, webpack, vue
	- json 형태의 객체 선언
		var 객체명 = {필드명:필드값, 매서드명:function(){} };
		기본적인 데이터 처리에서 많이 활용
		ajax를 활용(화면전환없이 데이터 로딩)하여 서버단에서 비동기적으로 
			데이터를 전송받아서 처리할 때는 json 형태로 활용한다.


# 학습목표
1. 내장된 객체의 종류를 알고, 각 객체의 특성과 사용하는 필드와 메서드를 파악하여 활용할 수 있다.
2. 생성자 객체의 선언과 활용에 대한 기본 키워드를 알고, 활용할 수 있다.
3. DOM 객체가 메모리가 어떻게 만들어지고, 어떤 순서로 처리해야하면 사용자 정의 객체를 통해서
	처리하는 방식을 이해하고 활용할 수 있다.

 주요핵심 keyword
1. Array, Date, String, Math
2. function 클래스명(매개변수1, 매개변수2){ this.속성=매개변수1; this.메서드명=function(){} }
3. css의 선택자 형식을 통한 document.querySelector(); document.querySelectorAll();

# 개념 및 원리이해

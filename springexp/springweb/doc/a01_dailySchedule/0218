1. vue를 style1객체(배경색상), style객체(글자색상)을 선언하여 배열로 속성 설정을 처리하는
		h3를 선언하세요
2. 클래스로 테두리/배경 색상을 설정하여, check box를 클릭 시 마다 적용여부 처리하게 하세요
3. web.xml의 contextConfigLocation 설정의 의미를 기술하세요
4. 컨테이너 설정에서 아래가 가지는 의미를 기술하세요.
		<property name="prefix" value="WEB-INF\views\"/>
		<property name="suffix" value=".jsp"/>
		<property name="order" value="1"/>
	컨트롤러 마지막 return의 view 호출을 선언할 때, 설정하는 옵션으로
	bean으로 선언한 viewer나 jsp view를 구분하기 위해,
	name="order"를 통해 우선 순위를 설정하고, prefix/suffix를 통해
	접두어와 접미어를 설정할 수 있다.
	
# 주요학슴범위
1. 스프링 게시판 개요와 주요기능 파악하기
2. vue 스타일 적용 예제

# 생각해봅시다.
1. vue에서 모델 메서드를 통한 동적기능의 구조는 무엇인가?
	모델 선정 : 핵심 데이터 처리를 위한 내용으로 v-model, methods과 함께
		변경 처리할 수 있다.
	v-model : 모델 데이터에 영향을 미칠 수 있는 입력 요소 객체에 보통 설정한다.
	methods, computed, watch 기타 기능 메서드를 통해서 위에 모델 데이터를 로직에
		의하여 입력 수정할 수 있다.
	이벤트 : 요소객체에서 선언한 이벤트 핸들러 메서드와 함께 효과적으로 동적인 구성을 처리할 수 있다.
		
2. 게시판에서 여러개 파일을 업로드 하기 위해서는 어떤 기능 설정과 처리가 필요할까?
	1) 다중의 파일을 업로드 하기 위해서는 MultiPartFile 객체가 배열이나 ArrayList 형태로
		VO또는 요청값으로 설정이 되어야 한다.
	2) 하나의 게시물과 연동되어 해당 게시물의 고유번호로 file정보를 저장하는 fno가 연동하여
		1:다관계로 저장되어야 한다.
	3) jqeury이벤트 처리를 통해서 여러개의 파일 추가/삭제가 가능하게 동적으로 처리하여야 한다.		

# 학습목표
1. 답변형 게시판에서 첨부파일 처리 구조를 이해하고 등록을 할 수 있다.
2. vue의 style, 모델, 기능메서드를 통한 실제 적용예제를 통해서 여러가지 상황에 따라 예제를 구현할 수 있다
	
# 주요핵심 keyword	
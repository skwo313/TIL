# 주요학습범위
1. 스프링 DI 연습 (namespace, 1:1. 1:다관계)
2. 스프링 autowire 설정
3. 객체형 컨테이너형 설정

# 생각해봅시다.
1. 스프링 컨테이너의 autowiring이란 무엇일까?
	스프링 컨테이너를 쓰는 목적은 메모리를 올려놓고, 사용하면 로딩 속도에 대한 부담이
	줄기에 속도가 빨라진다.
	객체가 메모리에 로딩되어 있는 상황이기에 설정값에 의해, 객체들끼리 자동으로 연결을 할 수 있다.
		Controller(사원리스트)는 사원정보를 가져오는 Dao가 필요로 한다.
		자동으로 붙기 때문에 new 객체명()으로 객체를 생성하지 않고 처리가 가능하다.
		
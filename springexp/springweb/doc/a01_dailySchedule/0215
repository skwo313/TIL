# 생각해봅시다.
1. vue에서 이벤트 처리는 어떤 방식으로 처리되는가?
	기본 js, jquery
		<h1 onclick="핸들러함수">
		$("h1").click(function(){ 핸들러함수});
	vue
		var vm = new Vue({
			method:{
				함수명:function(){
					// 이벤트 핸들러 함수로 이용..
					// 다른 함수나 모델데이터를 this.모델명 변경을 통해 데이터 처리
					// ajax를 통해서 비동기적으로 서버에 데이터 접근
					// 접근한 데이터를 모델값으로 할당
					// 화면에 v-for구문으로 처리된 내용 출력..
				}
			}
		});